import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Atheist.OG | Omkar Gopale's Portfolio",
  description: "Personal portfolio website of Omkar Gopale, showcasing Interdisciplinary work in Art, Writing, and Software Development.",
  openGraph: {
    title: "Atheist.OG Portfolio",
    description: "Projects, skills, and contact information of Omkar Gopale, an Interdisciplinary Architect",
    type: "website",
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
